maxTime(1000).

loadModule(p2p).

p2pJoin(lending, 4100).

fluent(money(A)).
initially(money(10)).
action(lend).

p2pReceive(lending, P, borrow, T1, T2) ->
  lend(T2, T3),
  p2pSend(lending, P, lend).

updates(lend, money(A), money(A - 10)).
